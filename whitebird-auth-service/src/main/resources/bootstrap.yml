spring:
  application:
    name: whitebird-auth-service
  cloud:
    config:
      discovery:
        service-id: whitebird-config-service
        enabled: true
feign:
  client:
    config:
      default:
        loggerLevel: FULL