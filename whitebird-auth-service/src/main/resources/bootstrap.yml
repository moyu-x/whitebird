spring:
  application:
    name: whitebird-auth-service
  cloud:
    config:
      discovery:
        service-id: whitebird-config-service
        enabled: true
    consul:
      host: ${CONSUL_HOST}
      discovery:
        heartbeat:
          enabled: true
