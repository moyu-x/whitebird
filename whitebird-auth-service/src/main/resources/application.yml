spring:
  application:
    name: whitebird-auth-service
  datasource:
    url: jdbc:mysql://localhost:10000/whitebird_auth_service
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    baseline-on-migrate: true
    check-location: false
server:
  port: 11010
logging:
  level:
    org.springframework.security: debug
