spring:
  application:
    name: whitebird-spring-boot-admin-service
  boot:
    admin:
      discovery:
        # 忽略服务注册信息
        ignored-services: consul
  cloud:
    consul:
      host: ${CONSUL_HOST}
      discovery:
        heartbeat:
          enabled: true
server:
  port: 11013
