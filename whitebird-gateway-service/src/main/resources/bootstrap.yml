spring:
  application:
    name: whitebird-gateway-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: whitebird-config-service
    consul:
      host: ${CONSUL_HOST}
      discovery:
        heartbeat:
          enabled: true
